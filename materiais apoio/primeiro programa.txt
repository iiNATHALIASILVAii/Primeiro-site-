#include <stdio.h> //biblioteca de comunicação com o usuário 
#include <stdlib.h> //biblioteca de alocação de espaço de memória
#include <locale.h> //biblioteca de alocação de texto por região

int main ()
{
	int opcao=0; //Definindo as variáveis
	int laco=1;
	
	for(laco=1;laco=1;)//Laço de repetição
	{
	
		system("cls");
		setlocale(LC_ALL, "Portuguese"); //Definindo linguagem 
	
		printf("### Cartório de alunos da EBAC ###\n\n"); //Barra + n é espaço; printf é a mensagem
		printf("Escolha a opção desejada do menu:\n\n");
		printf("\t1 - Registrar nomes\n");//Barra invertida + t traz um espaço antes da frase
		printf("\t2 - Consultar nomes\n");
		printf("\t3 - Deletar nomes\n\n");
		printf("Opção:");
	
		scanf("%d", &opcao); //armazenar variavel inteira - decisão usuario
	
		system("cls");//Fm do menu
	
		if(opcao==1)//dois iguais é para comparação um igual é atribuição - Inicio da seleção
		{
			printf("Você escolheu o registro de nomes!\n");
			system("pause");
			}
		if(opcao==2)
		{
			printf("Você escolheu consultar nomes!\n");
			system("pause");
		}
		if(opcao==3)
		{
			printf("Você escolheu deletar nomes!\n");
			system("pause");
		}
		if(opcao>=4 || opcao<=0) //barra pipe indica ou
		{
			printf("Essa opção não esta disponivel!\n");
			system("pause");
		}//Fim da seleção
    
	}

}
